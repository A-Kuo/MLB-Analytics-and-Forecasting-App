# Just what the scripts/backfill_*.py and scripts/ingest_team_news.py
# scripts import -- they never touch streamlit/plotly/pandas/fastapi/
# scikit-learn, so CI installs this instead of the full requirements.txt.
requests>=2.31
python-dotenv>=1.0
SQLAlchemy>=2.0
psycopg[binary]>=3.1
feedparser>=6.0
numpy>=1.26.0
pandas>=2.0.0
